<Window x:Class="MAL_Synchronizer.View.SetBrowserLocationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MAL_Synchronizer.View"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="MAL Synchronizer - Set Browser Path" Height="120" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25px"/>
            <RowDefinition Height="25px"/>
            <RowDefinition Height="25px"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Margin="4" Text="Firefox path: "/>
        <TextBox x:Name="tbFirefoxpath" Grid.Row="0" Grid.Column="1" Margin="2"/>
        <Button Grid.Row="0" Grid.Column="2" Margin="10,2" Content="Browse.." Click="Browse_Click"/>
        <Button Grid.Row="2" Grid.Column="2" Margin="10,2" Content="Save" Click="Save_Click"/>
    </Grid>
</Window>
